<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>qiktix uk - <%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <!-- Add more head elements like favicons, external stylesheets etc. here -->
</head>
<body>
    <!--
    Debug Info - User: <%= typeof user !== 'undefined' && user !== null ? user.email : 'No user' %>
    -->
    <header>
        <div class="header-container">
            <h1><a href="/">qiktix uk</a></h1>
            
            <!-- Search Bar -->
            <form action="/search" method="GET" class="search-form">
                <input type="search" name="q" placeholder="Search events, artists, venues..." aria-label="Search events">
                <button type="submit">Search</button>
            </form>

            <nav>
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/events">Events</a></li>
                    <li><a href="/help">Help</a></li>
                    <li><a href="/careers">Careers</a></li>
                    <li><a href="/press">Press</a></li>
                    <li><a href="/events/create">Create Event</a></li>
                    <% if (typeof user !== 'undefined' && user !== null) { %>
                        <li><a href="/account">My Account</a></li> <%# Link to user dashboard %>
                        <li>
                            <form action="/logout" method="POST" style="display: inline;">
                                <button type="submit" class="nav-logout-btn">Logout</button>
                            </form>
                        </li>
                    <% } else { %>
                        <li><a href="/login">Login</a></li>
                        <li><a href="/signup">Sign Up</a></li>
                    <% } %>
                    
                </ul>
            </nav>
        </div>
    </header>
    <div class="container">
        <main> 